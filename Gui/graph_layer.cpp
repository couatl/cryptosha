#include "graph_layer.h"
#include <QPainter>

/*GraphLayer::GraphLayer()
{
    Graph_Element * item = new Graph_Element;
    item->SetPins(4, 2);
    Graph_Element * item2 = new Graph_Element;
    item2->SetPins(2, 1);
    el_list.push_back(item);
    el_list.push_back(item2);
}
void GraphLayer::paint(QPainter *painter, const QStyleOptionGraphicsItem *option, QWidget *widget)
{
    /*for (auto i = el_list.begin(); i != el_list.begin(); ++i)
    {
        (*i)->paint(painter,option,widget);
    }
}

QRectF GraphLayer::boundingRect() const
{
    return QRectF(0, 0, 200, 20);
}*/
